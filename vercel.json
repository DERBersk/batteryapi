{
    "version": 2,
    "buildCommand": "python api/app.py -m batteryapi",
    "rewrites":[{
        "source":"/(.*)",
        "destination":"api/app.py"
    }],

    "functions": {
        "api/**/*.py": {
            "maxDuration": 30
      }
    }
  }
  